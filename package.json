{
  "name": "asydev-os",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name portoweb --branch master"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^6.2.0",
    "wrangler": "^4.131.1"
  }
}
